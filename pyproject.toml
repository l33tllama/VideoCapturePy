[project]
name = "videocapturepy"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.19.0",
    "PyQt6",
]

[project.optional-dependencies]
dev = [
    "pyinstaller",
]
